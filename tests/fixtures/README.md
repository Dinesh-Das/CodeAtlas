# Parser fixtures

Language and framework fixture repositories will be added with the Phase 2 Tree-sitter
adapters. Foundation integration fixtures are created as temporary Git repositories by the
test helpers so they exercise tracked, untracked, modified, and deleted working-tree state.
